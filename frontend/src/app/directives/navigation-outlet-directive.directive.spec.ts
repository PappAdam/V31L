import { NavigationOutletDirectiveDirective } from './navigation-outlet-directive.directive';

describe('NavigationOutletDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigationOutletDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
